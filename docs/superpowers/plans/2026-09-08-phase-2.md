# Phase 2: Separate Clarification Page

## Sources
- Root DESIGN.md: cream, ink, ash and blue; Cormorant + Inter; flat surfaces.
- docs/PRD-AnyMD-Product.md, Feature 2 and Phase 2: core questions, conditional follow-ups, quiet completeness line, temporary answers.
- User correction: hero input navigates to /clarify; all new motion uses GSAP, reduced motion is instant; no persistence or idea in URLs.
- Next 16.3.4 bundled docs: app/getting-started/layouts-and-pages and app/api-reference/functions/use-router. Shared root layouts preserve client state across navigation.
- GSAP React guidance: scoped gsap.context and matchMedia, reverted on cleanup.
- shadcn registry source: https://ui.shadcn.com/r/styles/new-york-v4/radio-group.json (MIT), adapted to @radix-ui/react-radio-group and existing CSS tokens.

## Implementation
1. Add pure question/branch/validation functions and tests. Nine core questions plus browser, mobile OS, and role follow-ups. Prune hidden answers and completion markers immediately.
2. Put a small memory-only DraftProvider in the root layout. Preserve original idea validation; clear clarification on changed idea submission, ignoring whitespace-only edits.
3. Replace inline handoff with animated route navigation. Build /clarify with native forms, labelled radios, stable question IDs, Back/Next, review/edit, and missing-draft state.
4. Animate stage opacity/transform and progress scaleX with scoped GSAP and cleanup. Lock transitions against duplicate clicks; focus the new heading after entry.
5. Correct landing preview copy. Run tests, lint, build; start port 3000 for parent browser QA. Leave root PRD checklist to parent after QA.

## Scope Boundary
Review is the end of Phase 2. Skill selection, AI generation, server sessions, billing and exports remain unimplemented. No localStorage, sessionStorage, cookies, private URL payloads, or third-party requests containing drafts.
